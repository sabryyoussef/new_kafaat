<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        
        <!-- Portal users can only see their own registrations -->
        <record id="student_registration_portal_rule" model="ir.rule">
            <field name="name">Student Registration: Portal User Access</field>
            <field name="model_id" ref="model_student_registration"/>
            <field name="domain_force">[('email', '=', user.email)]</field>
            <field name="groups" eval="[(4, ref('base.group_portal'))]"/>
            <field name="perm_read" eval="True"/>
            <field name="perm_write" eval="True"/>
            <field name="perm_create" eval="True"/>
            <field name="perm_unlink" eval="False"/>
        </record>
        
        <!-- Managers and Agents can see all registrations -->
        <record id="student_registration_manager_agent_rule" model="ir.rule">
            <field name="name">Student Registration: Manager and Agent Access</field>
            <field name="model_id" ref="model_student_registration"/>
            <field name="domain_force">[(1, '=', 1)]</field>
            <field name="groups" eval="[(4, ref('grants_training_suite_v19.group_manager')), (4, ref('grants_training_suite_v19.group_agent'))]"/>
            <field name="perm_read" eval="True"/>
            <field name="perm_write" eval="True"/>
            <field name="perm_create" eval="True"/>
            <field name="perm_unlink" eval="True"/>
        </record>
        
    </data>
</odoo>

